/* * This file is part of the ImageManager package. * * @author (c) Tim Shelburne <tim@dontlookstudios.com> * * For the full copyright and license information, please view the LICENSE * file that was distributed with this source code. */package dls.imagemanager {		import flash.display.Bitmap;		import org.osflash.signals.DeluxeSignal;	import org.osflash.signals.events.GenericEvent;		import dls.imagemanager.IImage;		/*	 * A class to represent and contain metadata about the image, as well as 	 * the bitmap image itself.	 */	public class Image implements IImage {				/*=========================================================*		 * PROPERTIES		 *=========================================================*/				private var _url:String;		public function get url():String {			return _url;		}				private var _bitmap:Bitmap;		public function get bitmap():Bitmap {			return _bitmap;		}		public function set bitmap(bitmap:Bitmap):void {			_bitmap = bitmap;			_bitmapSet.dispatch(new GenericEvent());		}				private var _bitmapSet:DeluxeSignal;		public function get bitmapSet():DeluxeSignal {			return _bitmapSet;		}				/*=========================================================*		 * FUNCTIONS		 *=========================================================*/		public function Image(url:String) {			_url = url;			_bitmapSet = new DeluxeSignal(this);		}			}	}